{
  "Redux_Map_Dispatch_to_Props": {
    "prefix": "rxmd",
    "body": [
      "const mapDispatchToProps = dispatch => {",
      "\treturn {",
      "\t\t${1}: () => dispatch(${1}(${2}))${0}",
      "\t};",
      "}"
    ],
    "description": "Redux Map Dispatch to Props"
  },
  "Redux_Map_State_to_Props": {
    "prefix": "rxms",
    "body": [
      "const mapStateToProps = state => {",
      "\treturn {",
      "\t\t${1}: state.${0}",
      "\t};",
      "}"
    ],
    "description": "Redux Map State to Props"
  }
}
